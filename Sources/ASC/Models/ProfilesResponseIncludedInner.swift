//
// ProfilesResponseIncludedInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public enum ProfilesResponseIncludedInner: Codable, JSONEncodable, Hashable {
    case typeBundleId(BundleId)
    case typeCertificate(Certificate)
    case typeDevice(Device)

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typeBundleId(let value):
            try container.encode(value)
        case .typeCertificate(let value):
            try container.encode(value)
        case .typeDevice(let value):
            try container.encode(value)
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode(BundleId.self) {
            self = .typeBundleId(value)
        } else if let value = try? container.decode(Certificate.self) {
            self = .typeCertificate(value)
        } else if let value = try? container.decode(Device.self) {
            self = .typeDevice(value)
        } else {
            throw DecodingError.typeMismatch(Self.Type.self, .init(codingPath: decoder.codingPath, debugDescription: "Unable to decode instance of ProfilesResponseIncludedInner"))
        }
    }
}

